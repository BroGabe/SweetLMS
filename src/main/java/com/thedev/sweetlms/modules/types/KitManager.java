package com.thedev.sweetlms.modules.types;

import java.util.UUID;

public class KitManager {

    protected void assignKitToPlayer(UUID playerUUID) {
        // Uses ItemSerializer class to get the kit from a base64 String.
    }
}
